'''

1. Usage: Find shortest path cost from a single node(source node) to any other nodes in that graph
(点到面(==所有点)的最短距离算法)
2. data structureL priority queue (min_heap)
3. 解题思路
    3.1 initial state (start node)
    3.2 node expansion/generation rule:
    3.3 termintion condition: 所有点都计算完毕才停止，也就是p_queue 变空

'''